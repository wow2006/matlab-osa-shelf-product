%%
figure(6);
%imshow(routeIndex.shelves);
for i=1:ii
    dotColor = colorPlate(randi(100),:);
    ind = find(matchPoints(:,1)==i);
    positions = zeros(size(ind,1),2);
    positions = [matchPoints(ind,2) ]
    figure(6);hold on;plot(C_data(k,1), C_data(k,2), dotColor ,'*'); 
end